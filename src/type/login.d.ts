export interface LOGINTYPES {
  loginName: string;
  password: string;
  merchantCode: string;
  checkCode: string;
}
export interface PASSWORDFORM {
  oldPassWord: string;
  newPassWord: string;
  checkNewPass: string;
}
